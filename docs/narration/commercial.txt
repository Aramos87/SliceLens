Slice Lens.

Eighty-seven point eight percent.

That's the number you'd paste into a launch post.

Looks like a win.

Until you look at the prompts with a negation.

Fourteen percent.

Same model. Same eval.

The average was a blend. The average was a lie.

Introducing Slice Lens.

One click names the slice that was carrying the errors.

Then it rewrites the headline you already wanted to ship.

You'd have reported ninety-six percent.

That's the rest of the run — the part that was covering for the failure.

How? One page. One engine. Four named predicates.

A reviewer talks to a React app. The app talks to FastAPI.

The engine searches a tiny library: negation, units, numbers, rare nouns.

It hunts on a discover split. Then it asks a held-out confirm split: does this still hold?

No uploads. No API keys. No mysterious Cluster Three.

A cluster you cannot name is not a finding. A sentence is.

Watch.

Negation trap. One click.

Green screenshot. Rust rewrite. Confirmed.

And if it doesn't replicate? We keep the miss on screen.

Because an eval tool that can only celebrate... is a commercial.

Slice Lens is the other kind.

Don't ship the screenshot. Name the slice.
